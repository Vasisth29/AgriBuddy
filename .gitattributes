# Git LFS Configuration
# Most files don't need LFS - they're under 100MB
# Only uncomment if you have files > 100MB

# *.h5 filter=lfs diff=lfs merge=lfs -text  # Not needed - files are under 100MB
# *.csv filter=lfs diff=lfs merge=lfs -text  # Not needed - CSV files are small
# *.xlsx filter=lfs diff=lfs merge=lfs -text  # Not needed - Excel files are small

# If you need LFS for specific large files, add them here:
# large_file.bin filter=lfs diff=lfs merge=lfs -text
